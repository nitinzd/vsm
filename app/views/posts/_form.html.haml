%h1 New post
= simple_form_for @post do |f|
  .form-inputs
    = f.input :title, label: "Title"
    = f.input :tags, label: "Tags"
    %p{id: "multiple_tags"} Add multiple tags separated by comma. E.G. Science,Maths
    = f.input :description, label: "Description"
    = f.input :body, label: "Body"
  .form-actions
    = f.button :submit, class: 'btn btn-large btn-success'
    = link_to "Cancel", (@post.new_record? ? posts_path : post_path(@post))
